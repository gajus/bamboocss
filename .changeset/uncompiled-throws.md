---
'@bamboocss/generator': major
'@bamboocss/dev': major
---

Uncompiled `css()`, `cva()`, `sva()`, recipes, patterns, and `viewTransition()` throw. Class strings come from the Vite
compiler; `css.raw()` still returns a style object.
