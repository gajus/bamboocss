---
title: Using Ember
description: Current support status for Bamboo and Ember.
---

<Callout type="warning">Ember does not currently expose a supported Vite compiler integration for Bamboo.</Callout>

`@bamboocss/postcss`, `bamboo init --postcss`, and a stylesheet generated by `cssgen` do not provide an alternative.
Generated `css()`, recipes, and patterns are compile-time APIs and throw if a Vite build did not replace their calls.

If your Ember build gains a Vite configuration you control, follow [Using Vite](/docs/installation/vite).
